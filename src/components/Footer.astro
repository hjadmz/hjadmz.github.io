<footer class="focus-element border-t border-[color:color-mix(in_srgb,var(--fg)_10%,transparent)] py-12">
  <div class="reading-column w-full flex flex-col items-center gap-6 text-center">
    <div>
      <a href="mailto:henryjosephadams@icloud.com" class="text-[var(--step-0)] text-[var(--fg)] link-subtle">
        henryjosephadams@icloud.com
      </a>
    </div>
    <div class="text-[var(--fg-muted)] text-xs font-mono flex flex-col md:flex-row items-center gap-2 md:gap-4">
      <span>FAITH . COVENANT-LOYALTY . SUBSTANCE-FREE</span>
      <span class="hidden md:inline opacity-30">|</span>
      <span id="footer-clock" class="opacity-60"></span>
      <span class="hidden md:inline opacity-30">|</span>
      <a href="/spec" class="link-subtle opacity-40 hover:opacity-100 transition-opacity duration-300">
        SPEC
      </a>
    </div>
  </div>
</footer>

<script>
  const clockEl = document.getElementById('footer-clock');
  if (clockEl) {
    const fmt = new Intl.DateTimeFormat('en-US', {
      hour: '2-digit',
      minute: '2-digit',
      second: '2-digit',
      timeZoneName: 'short',
    });
    const tick = () => { clockEl.textContent = fmt.format(new Date()); };
    tick();
    setInterval(tick, 1000);
  }
</script>
